# ADR 约定

ADR 是 Architecture Decision Record，用于记录长期架构决定。

## 什么时候写 ADR

以下情况需要 ADR：

- 改变架构边界。
- 选择或拒绝一个重要替代方案。
- 影响长期迁移路径。
- 引入难以回退的设计约束。

## 模板

每个 ADR 使用以下结构：Status、Context、Decision、Alternatives considered、Consequences。

## 编号规则

使用 `ADR-XXXX-short-name.md`。编号一旦发布，不要复用。
