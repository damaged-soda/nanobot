# 计划文件约定

计划文件是可执行的设计文档。它应该足够具体，使 Codex 或人类协作者不依赖聊天记录也能推进工作。

## 模板

每个计划至少包含：Goal、Background、Scope、Non-goals、Architecture impact、Steps、Acceptance checks、Rollback / kill switch、Docs to update、Exit criteria。

## 计划编号

使用 `PLAN-XXX-short-name.md`。编号一旦发布，不要复用。
